class Test {
  static int x;
  int k;
  
  // constructor with 2 args
  public Test()
  {

  }
  
  public static void main(String[] args) {
    System.out.println("General test code for stacks and strings");
    Stack<Integer> stack = new Stack<Integer>(); // in comments below we have some behind the scenes casting
    s.push(17); //s.push(new Integer(17));
    int a = s.pop();//int a = s.pop().intValue();

    //bottom line is that client code can use generic stack for any type of data
  }
}
